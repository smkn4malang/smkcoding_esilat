<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dycode.edu.vavibar">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="E_Silat"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".splash"
            android:label="E_Silat"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BeginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".lupaActivity" />
        <activity android:name=".signupActivity" />
        <activity android:name=".homeActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".isi_latihan" />
        <activity android:name=".isi_sejarah" />
        <activity android:name=".isi_seni" />
        <activity android:name=".isi_tanding"></activity>
    </application>

</manifest>